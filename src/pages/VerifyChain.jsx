/**
 * VerifyChain — placeholder screen (Stage 1).
 * Will implement SHA-256 chain verification in Stage 3.
 */
export default function VerifyChain() {
  return (
    <main className="main-content" id="main">
      <div className="page-header">
        <h1>Verify Chain</h1>
        <p>Recompute and validate the SHA-256 integrity chain for all entries.</p>
      </div>

      <div className="card">
        <p className="empty-state" style={{ padding: '2rem 0' }}>
          Chain verification will be implemented in Stage 3.
        </p>
      </div>
    </main>
  )
}
